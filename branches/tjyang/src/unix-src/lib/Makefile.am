#what: bin_PROGRAMS is a list of binary program. This word is a reserved keyword.
bin_PROGRAMS = hello

#what: ?_SOURCES  is a list of C source and header files  to generate a binary(hello) 
hello_SOURCES = main.c
