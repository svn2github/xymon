$ \textbf{if grep -q 'i have a gub' *}
> \textbf{then}
> \textbf{  result=bad}
> \textbf{else}
> \textbf{  result=good}
> \textbf{fi}
$ \textbf{echo this revision is $result}
this revision is bad
$ \textbf{hg bisect --$result}
hg bisect: option --bad not recognized
hg bisect [help|init|reset|next|good|bad]

bisect extension: dichotomic search in the DAG of changesets
for subcommands see "hg bisect help"
